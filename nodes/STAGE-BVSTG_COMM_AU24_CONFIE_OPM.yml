fileVersion: 1
id: 584f0a58-032f-458f-8d28-a5b6ad81023a
name: BVSTG_COMM_AU24_CONFIE_OPM
operation:
  config:
    insertStrategy: INSERT
    postSQL: ""
    preSQL: ""
    testsEnabled: true
    truncateBefore: true
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: STAGE
  materializationType: table
  metadata:
    appliedNodeTests: []
    columns:
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 3309d3a1-0551-4b76-8052-e43d3597e38e
          stepCounter: 584f0a58-032f-458f-8d28-a5b6ad81023a
        config: {}
        dataType: VARCHAR(50)
        defaultValue: ""
        description: ""
        name: BVSTG_COMM_AU24_CONFIE_OPM_ID
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: md5(ifnull(to_varchar(bvstg_comm_au6_store_evaluation.monthyear), '-') || ifnull(bvstg_comm_au6_store_evaluation.sg_description, '-'))
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 3f01632c-b711-4419-9137-70cbde8f1d3b
          stepCounter: 584f0a58-032f-458f-8d28-a5b6ad81023a
        config: {}
        dataType: DATE
        defaultValue: ""
        description: ""
        name: MONTHYEAR
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: e27f9194-0544-4c3c-a6d3-40308e4c0d91
          stepCounter: 584f0a58-032f-458f-8d28-a5b6ad81023a
        config: {}
        dataType: NUMBER(38,9)
        defaultValue: ""
        description: ""
        name: OPEXP
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: b5b57f97-f131-45ef-90b9-0005592c9854
          stepCounter: 584f0a58-032f-458f-8d28-a5b6ad81023a
        config: {}
        dataType: VARCHAR(24)
        defaultValue: ""
        description: ""
        name: SG_DESCRIPTION
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 82074845-e323-40d4-9495-f151cd5569e5
          stepCounter: 584f0a58-032f-458f-8d28-a5b6ad81023a
        config: {}
        dataType: TIMESTAMP_NTZ
        defaultValue: ""
        description: Date and time the row was created in the data warehouse.
        isSystemCreateDate: true
        name: DSS_CREATE_TIME
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 26b89645-d4ac-41eb-a972-2c00654f76a6
          stepCounter: 584f0a58-032f-458f-8d28-a5b6ad81023a
        config: {}
        dataType: TIMESTAMP_NTZ
        defaultValue: ""
        description: Date and time the row was updated in the data warehouse.
        isSystemUpdateDate: true
        name: DSS_UPDATE_TIME
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          BVSTG_COMM_AU13_CONFIE_OPM: 07e507c3-0258-45d9-8730-7164c617bda7
          BVSTG_COMM_AU18_CONFIE_OPM: ca04ee73-22e6-4bd5-a31f-6847285e842e
          BVSTG_COMM_AU23_CONFIE_OPM: 3e670fb7-a614-4c88-b641-c779016a0c07
          BVSTG_COMM_AU6_STORE_EVALUATION: f6c32513-0428-4633-9c49-a994fcaa3605
          BVSTG_COMM_AU22_CONFIE_OPM: e800ef72-4426-4965-b647-c20367974a49
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU13_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU18_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU23_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU6_STORE_EVALUATION
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU22_CONFIE_OPM
        join:
          joinCondition: |-
            EXTENDED
              FROM ((
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT bvstg_comm_au1.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END AS values_month,
                                   'MGA_EBITDA_Budget' AS sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear ,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM (
                                            SELECT UD2ID,
                                                   UD1ID,
                                                   yearid,
                                                   accountid,

                                             MONTH,
                                                   sum(ifnull(values_month,0)) Values_Month
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU13_CONFIE_OPM') }} UNPIVOT (values_month FOR MONTH IN (M1VALUE,M2VALUE,M3VALUE,M4VALUE,M5VALUE,M6VALUE, M7VALUE,M8VALUE,M9VALUE,M10VALUE,M11VALUE,M12VALUE))
                                             GROUP BY UD2ID,
                                                      UD1ID,
                                                      yearid,
                                                      accountid,

                                                MONTH
                                           )
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU18_CONFIE_OPM') }}
                                             WHERE sg_description= 'MGA_Revenue_Budget'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU23_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) bvstg_comm_au1
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT bvstg_comm_au2.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END AS values_month,
                                   'MGA_EBITDA_Actual' AS sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear ,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM (
                                            SELECT UD2ID,
                                                   UD1ID,
                                                   yearid,
                                                   accountid,

                                             MONTH,
                                                   sum(ifnull(values_month,0)) Values_Month
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU6_STORE_EVALUATION') }} UNPIVOT (values_month FOR MONTH IN (M1VALUE,M2VALUE,M3VALUE,M4VALUE,M5VALUE,M6VALUE, M7VALUE,M8VALUE,M9VALUE,M10VALUE,M11VALUE,M12VALUE))
                                             GROUP BY UD2ID,
                                                      UD1ID,
                                                      yearid,
                                                      accountid,

                                                MONTH
                                           )
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU18_CONFIE_OPM') }}
                                             WHERE sg_description= 'MGA_Revenue_Actual'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU22_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) bvstg_comm_au2
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (SELECT monthyear,
                   sum(values_month) OpExp,
                   sg_description
              FROM ( SELECT bvstg_comm_au3.monthyear,
                   ud2id,
                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                        ELSE values_month
                         END AS values_month,
                   'Retail_EBITDA_Budget' AS sg_description
              FROM ( SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear ,
                   ifnull(sum(values_month),0) values_month,
                   ud2id
              FROM (
                    SELECT UD2ID,
                           UD1ID,
                           yearid,
                           accountid,

                     MONTH,
                           sum(ifnull(values_month,0)) Values_Month
                      FROM {{ ref('STAGE','BVSTG_COMM_AU13_CONFIE_OPM') }} UNPIVOT (values_month FOR MONTH IN (M1VALUE,M2VALUE,M3VALUE,M4VALUE,M5VALUE,M6VALUE, M7VALUE,M8VALUE,M9VALUE,M10VALUE,M11VALUE,M12VALUE))
                     GROUP BY UD2ID,
                              UD1ID,
                              yearid,
                              accountid,

                        MONTH
                   )
             WHERE concat(ud2id,ud1id) IN (
                    SELECT DISTINCT concat(ud2id,ud1id)
                      FROM {{ ref('STAGE','BVSTG_COMM_AU18_CONFIE_OPM') }}
                     WHERE sg_description= 'Retail_Revenue_Budget'
                   )
               AND accountid IN (SELECT DISTINCT accountid
              FROM
        name: BVSTG_COMM_AU24_CONFIE_OPM
        noLinkRefs: []
  name: BVSTG_COMM_AU24_CONFIE_OPM
  overrideSQL: false
  schema: ""
  sqlType: Stage
  type: sql
  version: 1
type: Node
