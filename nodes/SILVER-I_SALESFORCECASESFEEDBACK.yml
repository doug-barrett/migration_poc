name: I_SALESFORCECASESFEEDBACK
id: 1ccfa691-8cff-12a6-b4f3-8a121a5fb283
fileVersion: 1
type: Node
operation:
  locationName: SILVER
  name: I_SALESFORCECASESFEEDBACK
  sqlType: View
  type: sql
  version: 1
  description: Salesforce Cases Feedback
  materializationType: view
  isMultisource: false
  metadata:
    sourceMapping:
      - name: source1
        join:
          joinCondition: 'FROM {{ ref(''SILVER'', ''I_SALESFORCECASES'') }}'
        dependencies:
          - locationName: SILVER
            nodeName: I_SALESFORCECASES
    columns:
      - name: FEEDBACKAREA
        dataType: VARCHAR(64)
        description: Feedback Area
        columnReference:
          stepCounter: '1'
          columnCounter: '1'
        transform: '--ONLY NEED THE FIRST VALUE AS COULD BE MULTI-SELECT UNTIL THE CHANGE THIS TO SINGLE SELECT CASE 		WHEN I_SALESFORCECASES.FEEDBACKAREA = ''[]'' THEN ''Not Applicable'' 		ELSE CASE 				WHEN POSITION('' IN '', I_SALESFORCECASES.FEEDBACKAREA) < 1 THEN LTRIM(RTRIM(REPLACE(REPLACE(REPLACE(I_SALESFORCECASES.FEEDBACKAREA, ''"'', ''''), '']'', ''''), ''['', ''''))) 				ELSE LEFT( 					LTRIM(RTRIM(REPLACE(REPLACE(REPLACE(I_SALESFORCECASES.FEEDBACKAREA, ''"'', ''''), '']'', ''''), ''['', ''''))) 					, -1 + POSITION('' IN '', LTRIM(RTRIM(REPLACE(REPLACE(REPLACE(I_SALESFORCECASES.FEEDBACKAREA, ''"'', ''''), '']'', ''''), ''['', ''''))) 					)) 			END 	END'
      - name: TYPEOFCOMPLAINT
        dataType: VARCHAR(64)
        description: Type Of Complaint
        columnReference:
          stepCounter: '1'
          columnCounter: '2'
        transform: '"I_SALESFORCECASES"."TYPEOFCOMPLAINT"'
