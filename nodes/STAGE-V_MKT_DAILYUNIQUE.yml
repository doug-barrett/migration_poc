fileVersion: 1
id: 71eaa6f9-ec53-4ae4-81a2-7f0d488812dc
name: V_MKT_DAILYUNIQUE
operation:
  config:
    insertStrategy: UNION
    selectDistinct: false
  customOverride: false
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: STAGE
  materializationType: view
  metadata:
    appliedNodeTests: []
    columns:
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 2a4cfbff-8d2e-4aa1-8aa1-b753bb149154
          stepCounter: 71eaa6f9-ec53-4ae4-81a2-7f0d488812dc
        config: {}
        dataType: INTEGER
        defaultValue: ""
        description: call_id
        name: CALL_ID
        nullable: false
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 0586b056-1fa7-4dc0-a512-518795024427
          stepCounter: 71eaa6f9-ec53-4ae4-81a2-7f0d488812dc
        config: {}
        dataType: INTEGER
        defaultValue: ""
        description: isDailyUnique
        name: ISDAILYUNIQUE
        nullable: false
        sourceColumnReferences:
          - columnReferences: []
            transform: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          V_MKT_DAILYUNIQUE: 71eaa6f9-ec53-4ae4-81a2-7f0d488812dc
          BVSTG_MKT_AU1B_CALL_LOG_CARS: b98992e8-60b3-4d75-afdb-839a1cb9e85b
        customSQL:
          customSQL: |-
            {{ stage('SQL Override') }}
            CREATE OR REPLACE VIEW {{ ref_no_link('STAGE','V_MKT_DAILYUNIQUE') }} AS SELECT DISTINCT a.call_id,
                   1 isDailyUnique
              FROM (
                    SELECT call_id,
                           (RANK() OVER (PARTITION BY caller_number,TO_DATE(Call_Start) ORDER BY Call_Start)) isDailyUnique
                      FROM {{ ref('STAGE','BVSTG_MKT_AU1B_CALL_LOG_CARS') }}
                     WHERE isNonUnique = 1
                       AND ifnull(caller_number,'')<>''
                   )a
             WHERE a.isDailyUnique = 1
               AND a.call_id NOT IN (
                    SELECT b.call_id
                      FROM (
                            SELECT call_id,
                                   RANK() OVER(PARTITION BY TO_DATE(Call_Start),caller_Number ORDER BY CASE WHEN Call_Status IN('ANSWER','SYSTEM CONNECT') THEN 1 ELSE 2 END, Call_Start, call_id) Ranking
                              FROM {{ ref('STAGE','BVSTG_MKT_AU1B_CALL_LOG_CARS') }}
                           ) b
                     WHERE b.Ranking>1
                   )
        dependencies:
          - locationName: STAGE
            nodeName: BVSTG_MKT_AU1B_CALL_LOG_CARS
        join:
          joinCondition: |-
            -- Nodes used in the SQL override:
            {{ ref('STAGE','BVSTG_MKT_AU1B_CALL_LOG_CARS') }}
        name: V_MKT_DAILYUNIQUE
        noLinkRefs: []
  name: V_MKT_DAILYUNIQUE
  overrideSQL: true
  schema: ""
  sqlType: View
  type: sql
  version: 1
type: Node
