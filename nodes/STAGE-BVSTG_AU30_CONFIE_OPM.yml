fileVersion: 1
id: 1d969797-aba9-4f7e-a025-5aff5f35acf2
name: BVSTG_AU30_CONFIE_OPM
operation:
  config:
    insertStrategy: INSERT
    postSQL: ""
    preSQL: ""
    testsEnabled: true
    truncateBefore: true
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: STAGE
  materializationType: table
  metadata:
    appliedNodeTests: []
    columns:
      - appliedColumnTests: {}
        columnReference:
          columnCounter: f6266954-401d-4cb3-8c01-4c905b994470
          stepCounter: 1d969797-aba9-4f7e-a025-5aff5f35acf2
        config: {}
        dataType: VARCHAR(50)
        defaultValue: ""
        description: ""
        name: BVSTG_AU30_CONFIE_OPM_ID
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: md5 (ifnull(bvstg_au14_confie_opm.monthyear,'1900-01-01') || bvstg_au14_confie_opm.sg_description)
      - appliedColumnTests: {}
        columnReference:
          columnCounter: edcc8898-3e26-491f-b3a1-352345d321b6
          stepCounter: 1d969797-aba9-4f7e-a025-5aff5f35acf2
        config: {}
        dataType: DATE
        defaultValue: ""
        description: ""
        name: MONTHYEAR
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: f29cbb1d-7fec-4fd5-af75-4763692c9721
          stepCounter: 1d969797-aba9-4f7e-a025-5aff5f35acf2
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: SG_DESCRIPTION
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: b4fc8bc1-ddd3-40aa-bfc7-da4824064c76
          stepCounter: 1d969797-aba9-4f7e-a025-5aff5f35acf2
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: OPEXP
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          BVSTG_AU14_CONFIE_OPM: c1a8a0e0-42be-409c-801d-52cd41e590b8
          BVSTG_AU8_CONFIE_OPM: 563d3662-3fcb-411e-b6cf-837d44795d1c
          BVSTG_AU6_CONFIE_OPM: 4067f2fe-ab93-4d53-b790-0a2ef5d759b5
          BVSTG_AU9_CONFIE_OPM: e4a55d8b-080b-49e5-8ffb-e149cb793a50
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: STAGE
            nodeName: BVSTG_AU14_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU8_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU6_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU9_CONFIE_OPM
        join:
          joinCondition: |-
            EXTENDED
              FROM ( SELECT *
              FROM (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table2.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'IT_0044' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU8_CONFIE_OPM') }}
                                             WHERE sg_description= 'IT_0044'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table2
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table3.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'IT_0049' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU8_CONFIE_OPM') }}
                                             WHERE sg_description= 'IT_0049'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table3
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table4.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'IT_0035' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU8_CONFIE_OPM') }}
                                             WHERE sg_description= 'IT_0035'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table4
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table6.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'SGA_Numerator' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU9_CONFIE_OPM') }}
                                             WHERE sg_description= 'SGA_Numerator'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table6
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (SELECT monthyear,
                   sum(values_month) OpExp,
                   sg_description
        name: BVSTG_AU30_CONFIE_OPM
        noLinkRefs: []
  name: BVSTG_AU30_CONFIE_OPM
  overrideSQL: false
  schema: ""
  sqlType: Stage
  type: sql
  version: 1
type: Node
