fileVersion: 1
id: 56890020-1c43-4a71-bf0c-7ed6c8bfd4ac
name: BVSTG_1AU13_CSPR
operation:
  config:
    insertStrategy: INSERT
    postSQL: ""
    preSQL: ""
    testsEnabled: true
    truncateBefore: true
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: STAGE
  materializationType: table
  metadata:
    appliedNodeTests: []
    columns:
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 461c90e9-64e0-49ef-8195-3bc2f1f4779e
          stepCounter: 56890020-1c43-4a71-bf0c-7ed6c8bfd4ac
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: BVSTG_1AU13_CSPR_ID
        nullable: false
        sourceColumnReferences:
          - columnReferences: []
            transform: md5(v_dlbv_osm_relationship_current.childid)
      - appliedColumnTests: {}
        columnReference:
          columnCounter: af9618b0-39c7-4312-954b-346e82c14723
          stepCounter: 56890020-1c43-4a71-bf0c-7ed6c8bfd4ac
        config: {}
        dataType: NUMBER(38,0)
        defaultValue: ""
        description: ""
        name: CHILDID
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: 62abea1a-7da2-4f82-b76d-6aaa584504db
                stepCounter: 349f2726-4aad-4830-99aa-6e1bf30716a8
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: b5118dc5-0cc9-430d-8e40-1e668b6ef8e5
          stepCounter: 56890020-1c43-4a71-bf0c-7ed6c8bfd4ac
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: CHILD_DESCR
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 25150573-f13e-44bd-b73d-5a83d4a2a48a
          stepCounter: 56890020-1c43-4a71-bf0c-7ed6c8bfd4ac
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: ACCOUNTS_MAPPED
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: cf481fc1-7741-466e-997f-4b08e39585de
          stepCounter: 56890020-1c43-4a71-bf0c-7ed6c8bfd4ac
        config: {}
        dataType: TIMESTAMP_NTZ
        defaultValue: ""
        description: Date and time the row was created in the data warehouse.
        isSystemCreateDate: true
        name: DSS_CREATE_TIME
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 804c3573-f9e2-4134-b79c-adba2d4806ef
          stepCounter: 56890020-1c43-4a71-bf0c-7ed6c8bfd4ac
        config: {}
        dataType: TIMESTAMP_NTZ
        defaultValue: ""
        description: Date and time the row was updated in the data warehouse.
        isSystemUpdateDate: true
        name: DSS_UPDATE_TIME
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          V_DLBV_OSM_MEMBER_CURRENT: 5f0cc3ae-d915-4c49-bf42-e20f12b4a385
          V_DLBV_OSM_RELATIONSHIP_CURRENT: 349f2726-4aad-4830-99aa-6e1bf30716a8
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: STAGE
            nodeName: V_DLBV_OSM_MEMBER_CURRENT
          - locationName: STAGE
            nodeName: V_DLBV_OSM_RELATIONSHIP_CURRENT
        join:
          joinCondition: |-
            EXTENDED
              FROM ( SELECT *
              FROM ( SELECT t1.childid,
                   t2.description AS child_descr,
                   Accounts_Mapped
              FROM ( SELECT DISTINCT memberid AS childid,
                   description AS Accounts_Mapped
              FROM (
                    SELECT DISTINCT memberid,
                           description
                      FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                     WHERE name = 'CompExpOp'
                     UNION SELECT DISTINCT childid,
                           (
                            SELECT DISTINCT description
                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                             WHERE name = 'CompExpOp'
                           ) AS description
                      FROM (
                            SELECT DISTINCT childid
                              FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                             WHERE parentid IN (
                                    SELECT DISTINCT memberid
                                      FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                                     WHERE name = 'CompExpOp'
                                   )
                             UNION SELECT DISTINCT b.childid childid
                              FROM (
                                    SELECT *
                                      FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                                     WHERE parentid IN (
                                            SELECT DISTINCT memberid
                                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                                             WHERE name = 'CompExpOp'
                                           )
                                   ) a
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} b
                                ON a.childid=b.parentid
                             UNION SELECT DISTINCT c.childid childid
                              FROM (
                                    SELECT *
                                      FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                                     WHERE parentid IN (
                                            SELECT DISTINCT memberid
                                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                                             WHERE name = 'CompExpOp'
                                           )
                                   ) a
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} b
                                ON a.childid=b.parentid
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} c
                                ON b.childid=c.parentid
                             UNION SELECT DISTINCT d.childid childid
                              FROM (
                                    SELECT *
                                      FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                                     WHERE parentid IN (
                                            SELECT DISTINCT memberid
                                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                                             WHERE name = 'CompExpOp'
                                           )
                                   ) a
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} b
                                ON a.childid=b.parentid
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} c
                                ON b.childid=c.parentid
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} d
                                ON c.childid=d.parentid
                           )
                   )
             UNION (
                    SELECT DISTINCT memberid,
                           description
                      FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                     WHERE name = 'RentExpensesOp'
                     UNION SELECT DISTINCT childid,
                           (
                            SELECT DISTINCT description
                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                             WHERE name = 'RentExpensesOp'
                           ) AS description
                      FROM (
                            SELECT DISTINCT childid
                              FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                             WHERE parentid IN (
                                    SELECT DISTINCT memberid
                                      FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                                     WHERE name = 'RentExpensesOp'
                                   )
                             UNION SELECT DISTINCT b.childid childid
                              FROM (
                                    SELECT *
                                      FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                                     WHERE parentid IN (
                                            SELECT DISTINCT memberid
                                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                                             WHERE name = 'RentExpensesOp'
                                           )
                                   ) a
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} b
                                ON a.childid=b.parentid
                             UNION SELECT DISTINCT c.childid childid
                              FROM (
                                    SELECT *
                                      FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                                     WHERE parentid IN (
                                            SELECT DISTINCT memberid
                                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                                             WHERE name = 'RentExpensesOp'
                                           )
                                   ) a
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} b
                                ON a.childid=b.parentid
                              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} c
                                ON b.childid=c.parentid
                           )
                   )
             UNION (SELECT DISTINCT memberid,
                   description
              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
             WHERE name = 'Utilities'
             UNION SELECT DISTINCT childid,
                   (
                    SELECT DISTINCT description
                      FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                     WHERE name = 'Utilities'
                   ) AS description
              FROM ( SELECT DISTINCT childid
              FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
             WHERE parentid IN (
                    SELECT DISTINCT memberid
                      FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                     WHERE name = 'Utilities'
                   )
             UNION SELECT DISTINCT b.childid childid
              FROM (
                    SELECT *
                      FROM {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }}
                     WHERE parentid IN (
                            SELECT DISTINCT memberid
                              FROM {{ ref('STAGE','V_DLBV_OSM_MEMBER_CURRENT') }}
                             WHERE name = 'Utilities'
                           )
                   ) a
              LEFT JOIN {{ ref('STAGE','V_DLBV_OSM_RELATIONSHIP_CURRENT') }} b
                ON a.childid=b.parentid
             UNION SELECT DISTINCT c.childid childid
              FROM (SELECT *
              FROM [TABLEOWNER].[V_D
        name: BVSTG_1AU13_CSPR
        noLinkRefs: []
  name: BVSTG_1AU13_CSPR
  overrideSQL: false
  schema: ""
  sqlType: Stage
  type: sql
  version: 1
type: Node
