fileVersion: 1
id: 9b5153b2-247f-4909-890e-6ae78025c058
name: BVSTG_AU31_CONFIE_OPM
operation:
  config:
    insertStrategy: INSERT
    postSQL: ""
    preSQL: ""
    testsEnabled: true
    truncateBefore: true
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: STAGE
  materializationType: table
  metadata:
    appliedNodeTests: []
    columns:
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 5bbce5a5-da41-43dd-b01e-4b1ec5a32c25
          stepCounter: 9b5153b2-247f-4909-890e-6ae78025c058
        config: {}
        dataType: VARCHAR(50)
        defaultValue: ""
        description: ""
        name: BVSTG_AU31_CONFIE_OPM_ID
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: md5 (ifnull(bvstg_au14_confie_opm.monthyear,'1900-01-01') || bvstg_au14_confie_opm.sg_description)
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 3195aa40-46e9-4e6f-bb17-5d6fba714fa3
          stepCounter: 9b5153b2-247f-4909-890e-6ae78025c058
        config: {}
        dataType: DATE
        defaultValue: ""
        description: ""
        name: MONTHYEAR
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 96977b14-0bcf-4f28-bf95-21cddcf02759
          stepCounter: 9b5153b2-247f-4909-890e-6ae78025c058
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: SG_DESCRIPTION
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: b5beaae8-db70-455b-a625-a067c2b84a45
          stepCounter: 9b5153b2-247f-4909-890e-6ae78025c058
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: OPEXP
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          BVSTG_AU30_CONFIE_OPM: 1d969797-aba9-4f7e-a025-5aff5f35acf2
          BVSTG_AU27_CONFIE_OPM: 3e1d0830-a025-4f87-86a0-c1ccc8f457d8
          BVSTG_AU20_CONFIE_OPM: 307a189f-78c5-42bd-9f0a-e59267d5faea
          BVSTG_AU7_CONFIE_OPM: 6b5652ef-15b7-49c6-991f-d587e1481fc5
          BVSTG_AU14_CONFIE_OPM: c1a8a0e0-42be-409c-801d-52cd41e590b8
          BVSTG_AU22_CONFIE_OPM: 3f6be0dc-a974-48c2-adc7-af208b792c32
          BVSTG_AU6_CONFIE_OPM: 4067f2fe-ab93-4d53-b790-0a2ef5d759b5
          BVSTG_AU18_CONFIE_OPM: ca00e550-8fbf-4ae7-8608-605766fac19d
          BVSTG_AU11_CONFIE_OPM: bcf6bf92-3f4e-4cfd-b5bd-b5ce76430329
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: STAGE
            nodeName: BVSTG_AU30_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU27_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU20_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU7_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU14_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU22_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU6_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU18_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU11_CONFIE_OPM
        join:
          joinCondition: |-
            EXTENDED
              FROM ( SELECT *
              FROM (
                    SELECT MONTHYEAR,
                           OpExp,
                           SG_DESCRIPTION
                      FROM {{ ref('STAGE','BVSTG_AU30_CONFIE_OPM') }}
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table8.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'MGA_EBITDA_Budget' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU27_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU20_CONFIE_OPM') }}
                                             WHERE sg_description= 'MGA_Revenue_Budget'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU7_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table8
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table9.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'MGA_EBITDA_Actual' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU22_CONFIE_OPM') }}
                                             WHERE sg_description= 'MGA_Revenue_Actual'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table9
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table10.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'Retail_EBITDA_Budget' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU27_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU18_CONFIE_OPM') }}
                                             WHERE sg_description= 'Retail_Revenue_Budget'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU7_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table10
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (SELECT monthyear,
                   sum(values_month) OpExp,
                   sg_description
              FROM (SELECT table11.monthyear,
                   ud2id,
                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                        ELSE values_month
                         END values_month,
                   'Retail_EBITDA_Actual' sg_description
              FROM (SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                   ifnull(sum(values_month),0) values_month,
                   ud2id
              FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
             WHERE concat(ud2id,ud1id) IN (
                    SELECT DISTINCT concat(ud2id,ud1id)
                      FROM {{ ref('STAGE','BVSTG_AU11_CONFIE_OPM') }}
                     WHERE sg_description= 'Retail_Sales'
                   )
               AND accountid IN (
                    SELECT DISTINCT accountid
                      FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                     WHERE sg_description ='TotalOpExp'
                   )
               AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
             GROUP BY ud2id,
                      date(concat(YEAR
        name: BVSTG_AU31_CONFIE_OPM
        noLinkRefs: []
  name: BVSTG_AU31_CONFIE_OPM
  overrideSQL: false
  schema: ""
  sqlType: Stage
  type: sql
  version: 1
type: Node
