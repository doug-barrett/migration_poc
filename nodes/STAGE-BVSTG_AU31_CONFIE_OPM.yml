fileVersion: 1
id: 8923a555-4378-4ad2-90ea-b58dcd797502
name: BVSTG_AU31_CONFIE_OPM
operation:
  config:
    insertStrategy: INSERT
    postSQL: ""
    preSQL: ""
    testsEnabled: true
    truncateBefore: true
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: STAGE
  materializationType: table
  metadata:
    appliedNodeTests: []
    columns:
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 855fc99c-dd7e-48af-a01b-2a3b82bdeff9
          stepCounter: 8923a555-4378-4ad2-90ea-b58dcd797502
        config: {}
        dataType: VARCHAR(50)
        defaultValue: ""
        description: ""
        name: BVSTG_AU31_CONFIE_OPM_ID
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: md5 (ifnull(bvstg_au14_confie_opm.monthyear,'1900-01-01') || bvstg_au14_confie_opm.sg_description)
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 26cb1d7b-cd2b-4cf4-8c32-f00d85e079ee
          stepCounter: 8923a555-4378-4ad2-90ea-b58dcd797502
        config: {}
        dataType: DATE
        defaultValue: ""
        description: ""
        name: MONTHYEAR
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: b61e0ded-f7d3-4818-87bf-6890a849dc65
          stepCounter: 8923a555-4378-4ad2-90ea-b58dcd797502
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: SG_DESCRIPTION
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 3139bd26-81f0-42bf-910e-6ed1ae278d31
          stepCounter: 8923a555-4378-4ad2-90ea-b58dcd797502
        config: {}
        dataType: VARCHAR(150)
        defaultValue: ""
        description: ""
        name: OPEXP
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          BVSTG_AU30_CONFIE_OPM: 24524337-5b4e-4855-b4f8-d48a5d710c56
          BVSTG_AU27_CONFIE_OPM: 586d7548-383d-4f99-bda4-67ca17dcd90b
          BVSTG_AU20_CONFIE_OPM: c6c57dc9-d7b2-4539-af45-2cc1387d7c8c
          BVSTG_AU7_CONFIE_OPM: 82cbc4d4-7975-407e-979d-daffcdf2b40d
          BVSTG_AU14_CONFIE_OPM: 1e3809f3-5b2e-4d04-81cb-e5c74a20a826
          BVSTG_AU22_CONFIE_OPM: 4cdb58e6-1b2c-4ec8-80f2-4ba3629542a7
          BVSTG_AU6_CONFIE_OPM: 229d24ad-41fe-4baa-a289-031d65af0f47
          BVSTG_AU18_CONFIE_OPM: c3c5be51-46bb-45f3-96a6-1cdf4a0c05aa
          BVSTG_AU11_CONFIE_OPM: 5ad2510a-5745-4749-a365-df15d2c2148c
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: STAGE
            nodeName: BVSTG_AU30_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU27_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU20_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU7_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU14_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU22_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU6_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU18_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_AU11_CONFIE_OPM
        join:
          joinCondition: |-
            EXTENDED
              FROM ( SELECT *
              FROM (
                    SELECT MONTHYEAR,
                           OpExp,
                           SG_DESCRIPTION
                      FROM {{ ref('STAGE','BVSTG_AU30_CONFIE_OPM') }}
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table8.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'MGA_EBITDA_Budget' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU27_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU20_CONFIE_OPM') }}
                                             WHERE sg_description= 'MGA_Revenue_Budget'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU7_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table8
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table9.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'MGA_EBITDA_Actual' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU22_CONFIE_OPM') }}
                                             WHERE sg_description= 'MGA_Revenue_Actual'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table9
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT table10.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END values_month,
                                   'Retail_EBITDA_Budget' sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM {{ ref('STAGE','BVSTG_AU27_CONFIE_OPM') }}
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_AU18_CONFIE_OPM') }}
                                             WHERE sg_description= 'Retail_Revenue_Budget'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_AU7_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) table10
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (SELECT monthyear,
                   sum(values_month) OpExp,
                   sg_description
              FROM (SELECT table11.monthyear,
                   ud2id,
                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                        ELSE values_month
                         END values_month,
                   'Retail_EBITDA_Actual' sg_description
              FROM (SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear,
                   ifnull(sum(values_month),0) values_month,
                   ud2id
              FROM {{ ref('STAGE','BVSTG_AU14_CONFIE_OPM') }}
             WHERE concat(ud2id,ud1id) IN (
                    SELECT DISTINCT concat(ud2id,ud1id)
                      FROM {{ ref('STAGE','BVSTG_AU11_CONFIE_OPM') }}
                     WHERE sg_description= 'Retail_Sales'
                   )
               AND accountid IN (
                    SELECT DISTINCT accountid
                      FROM {{ ref('STAGE','BVSTG_AU6_CONFIE_OPM') }}
                     WHERE sg_description ='TotalOpExp'
                   )
               AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
             GROUP BY ud2id,
                      date(concat(YEAR
        name: BVSTG_AU31_CONFIE_OPM
        noLinkRefs: []
  name: BVSTG_AU31_CONFIE_OPM
  overrideSQL: false
  schema: ""
  sqlType: Stage
  type: sql
  version: 1
type: Node
