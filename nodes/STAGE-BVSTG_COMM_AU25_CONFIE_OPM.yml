fileVersion: 1
id: 169bc4b8-f26c-4643-b224-d5688ccc33cf
name: BVSTG_COMM_AU25_CONFIE_OPM
operation:
  config:
    insertStrategy: INSERT
    postSQL: ""
    preSQL: ""
    testsEnabled: true
    truncateBefore: true
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: STAGE
  materializationType: table
  metadata:
    appliedNodeTests: []
    columns:
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 17d1deb2-28e3-4db2-8c36-29b6c18db0e1
          stepCounter: 169bc4b8-f26c-4643-b224-d5688ccc33cf
        config: {}
        dataType: VARCHAR(50)
        defaultValue: ""
        description: ""
        name: BVSTG_COMM_AU25_CONFIE_OPM_ID
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: md5(ifnull(to_varchar(bvstg_comm_au6_store_evaluation.monthyear), '-') || ifnull(bvstg_comm_au6_store_evaluation.sg_description, '-'))
      - appliedColumnTests: {}
        columnReference:
          columnCounter: b255dcec-97d2-47d3-900b-d20a9ee6aeb9
          stepCounter: 169bc4b8-f26c-4643-b224-d5688ccc33cf
        config: {}
        dataType: VARCHAR(24)
        defaultValue: ""
        description: ""
        name: SG_DESCRIPTION
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: ce832921-4b53-4d6b-a7b2-827990ab1dae
          stepCounter: 169bc4b8-f26c-4643-b224-d5688ccc33cf
        config: {}
        dataType: DATE
        defaultValue: ""
        description: ""
        name: MONTHYEAR
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 5b2f4b8f-d928-4198-b8c7-451a1e2a5829
          stepCounter: 169bc4b8-f26c-4643-b224-d5688ccc33cf
        config: {}
        dataType: NUMBER(38,9)
        defaultValue: ""
        description: ""
        name: OPEXP
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 9f97e6a6-2e8d-49d6-9c14-41e61eebc355
          stepCounter: 169bc4b8-f26c-4643-b224-d5688ccc33cf
        config: {}
        dataType: TIMESTAMP_NTZ
        defaultValue: ""
        description: Date and time the row was created in the data warehouse.
        isSystemCreateDate: true
        name: DSS_CREATE_TIME
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: ""
      - appliedColumnTests: {}
        columnReference:
          columnCounter: 2a12b0ff-f627-479b-8c1c-694f5bdf40de
          stepCounter: 169bc4b8-f26c-4643-b224-d5688ccc33cf
        config: {}
        dataType: TIMESTAMP_NTZ
        defaultValue: ""
        description: Date and time the row was updated in the data warehouse.
        isSystemUpdateDate: true
        name: DSS_UPDATE_TIME
        nullable: true
        sourceColumnReferences:
          - columnReferences: []
            transform: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases:
          BVSTG_COMM_AU6_STORE_EVALUATION: a5996bd8-56d0-4828-b4b5-d4100341a294
          BVSTG_COMM_AU9_CONFIE_OPM: ac2c71c2-b845-4932-9b15-6327c95197d0
          BVSTG_COMM_AU22_CONFIE_OPM: 9263bd75-ec9f-4b75-9354-ef2b4bd56fa6
        customSQL:
          customSQL: ""
        dependencies:
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU6_STORE_EVALUATION
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU9_CONFIE_OPM
          - locationName: STAGE
            nodeName: BVSTG_COMM_AU22_CONFIE_OPM
        join:
          joinCondition: |-
            EXTENDED
              FROM ((
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT bvstg_au9.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END AS values_month,
                                   'TotalOPExp' AS sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear ,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM (
                                            SELECT UD2ID,
                                                   UD1ID,
                                                   yearid,
                                                   accountid,

                                             MONTH,
                                                   sum(ifnull(values_month,0)) Values_Month
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU6_STORE_EVALUATION') }} UNPIVOT (values_month FOR MONTH IN (M1VALUE,M2VALUE,M3VALUE,M4VALUE,M5VALUE,M6VALUE, M7VALUE,M8VALUE,M9VALUE,M10VALUE,M11VALUE,M12VALUE))
                                             GROUP BY UD2ID,
                                                      UD1ID,
                                                      yearid,
                                                      accountid,

                                                MONTH
                                           )
                                     WHERE ud2id IN (
                                            SELECT DISTINCT ud2id
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU9_CONFIE_OPM') }}
                                             WHERE sg_description= 'Retail_Sales'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU22_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) bvstg_au9
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (
                    SELECT monthyear,
                           sum(values_month) OpExp,
                           sg_description
                      FROM (
                            SELECT bvstg_au9_2.monthyear,
                                   ud2id,
                                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                                        ELSE values_month
                                         END AS values_month,
                                   'IT_0044' AS sg_description
                              FROM (
                                    SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear ,
                                           ifnull(sum(values_month),0) values_month,
                                           ud2id
                                      FROM (
                                            SELECT UD2ID,
                                                   UD1ID,
                                                   yearid,
                                                   accountid,

                                             MONTH,
                                                   sum(ifnull(values_month,0)) Values_Month
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU6_STORE_EVALUATION') }} UNPIVOT (values_month FOR MONTH IN (M1VALUE,M2VALUE,M3VALUE,M4VALUE,M5VALUE,M6VALUE, M7VALUE,M8VALUE,M9VALUE,M10VALUE,M11VALUE,M12VALUE))
                                             GROUP BY UD2ID,
                                                      UD1ID,
                                                      yearid,
                                                      accountid,

                                                MONTH
                                           )
                                     WHERE concat(ud2id,ud1id) IN (
                                            SELECT DISTINCT concat(ud2id,ud1id)
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU9_CONFIE_OPM') }}
                                             WHERE sg_description= 'IT_0044'
                                           )
                                       AND accountid IN (
                                            SELECT DISTINCT accountid
                                              FROM {{ ref('STAGE','BVSTG_COMM_AU22_CONFIE_OPM') }}
                                             WHERE sg_description ='TotalOpExp'
                                           )
                                       AND monthyear<=dateadd('day',-1,date_trunc('month',CURRENT_DATE))
                                     GROUP BY ud2id,
                                              date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01'))
                                   ) bvstg_au9_2
                           )
                     GROUP BY monthyear,
                              sg_description
                   )
            UNION ALL (SELECT monthyear,
                   sum(values_month) OpExp,
                   sg_description
              FROM ( SELECT bvstg_au9_3.monthyear,
                   ud2id,
                   CASE WHEN month(monthyear) <> 1 THEN values_month- lag(values_month) OVER (PARTITION BY ud2id ORDER BY monthyear)
                        ELSE values_month
                         END AS values_month,
                   'IT_0049' AS sg_description
              FROM ( SELECT DISTINCT date(concat(yearid,'-',replace(replace(MONTH,'M'),'VALUE'),'-01')) monthyear ,
                   ifnull(sum(values_month),0) values_month,
                   ud2id
              FROM (SELECT UD2ID,
                   UD1ID,
                   yearid,
                   accountid,

             MONTH,
                   sum(ifnull(values_month,0)) Values_Month
        name: BVSTG_COMM_AU25_CONFIE_OPM
        noLinkRefs: []
  name: BVSTG_COMM_AU25_CONFIE_OPM
  overrideSQL: false
  schema: ""
  sqlType: Stage
  type: sql
  version: 1
type: Node
