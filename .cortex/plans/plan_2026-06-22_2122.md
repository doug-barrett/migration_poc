---
created: 2026-06-22T21:22:16.285Z
session: 76fec7f5-4829-47aa-9490-e96534a04ed3
working_directory: /Users/dougy/work/POC/Northpower/migration_poc
---

# Rebuild Network Survey Datamart with Correct Coalesce YAML Structure\n\n## Context\n\nStudied the example nodes (CUSTOMER, NATION, REGION, STG_CUSTOMER, STG_ORDERS, DIM_CUSTOMER, FCT_ORDERS). Key patterns identified:\n\n**Source Nodes** (`type: sourceInput`):\n- `columnReference.columnCounter` = unique UUID per column\n- `columnReference.stepCounter` = the node's own `id`\n- `sourceColumnReferences: [{columnReferences: [], transform: \"\"}]` (empty - no upstream)\n- No `sourceMapping` section\n\n**Stage/Dim/Fact Nodes** (`type: sql`):\n- Same UUID pattern for `columnReference`\n- `sourceColumnReferences` links to upstream: `columnCounter` = the source column's UUID, `stepCounter` = the source node's `id`\n- `sourceMapping` contains:\n  - `aliases`: map of `NODE_NAME: node_id_uuid`\n  - `join.joinCondition`: full FROM/JOIN with `{{ ref('LOC', 'NAME') }} \"ALIAS\"`\n  - `dependencies`: list of `{locationName, nodeName}`\n  - `name`: same as this node's name\n  - `noLinkRefs: []`\n  - `customSQL: {customSQL: \"\"}`\n- System columns (surrogate key, version, current flag, dates) use `sourceColumnReferences: [{columnReferences: [], transform: \"...\"}]`\n- Dimension system columns: `isSurrogateKey`, `isSystemVersion`, `isSystemCurrentFlag`, `isSystemStartDate`, `isSystemEndDate`, `isSystemCreateDate`, `isSystemUpdateDate`\n- Fact system columns: `isSystemCreateDate`, `isSystemUpdateDate`\n\n## Implementation Steps\n\n### Step 1: Delete example nodes\nRemove the 8 example nodes in `nodes/` (BRONZE-CUSTOMER, BRONZE-NATION, BRONZE-ORDERS, BRONZE-REGION, SILVER-STG_CUSTOMER, SILVER-STG_ORDERS, SILVER-DIM_CUSTOMER, SILVER-FCT_ORDERS).\n\n### Step 2: Rewrite `scripts/parse_wherescape.py`\nComplete rewrite with correct YAML generation:\n\n**UUID Strategy:**\n- Each node gets a stable UUID (generated deterministically from name using `uuid.uuid5`)\n- Each column gets a stable UUID (generated from `node_name + col_name`)\n- This ensures `sourceColumnReferences` can cross-reference correctly\n\n**Node generation changes:**\n\n1. **Source nodes** (L_ loads):\n   - `type: sourceInput`, `sqlType: Source`\n   - Each column: `columnReference: {columnCounter: <col_uuid>, stepCounter: <node_uuid>}`\n   - `sourceColumnReferences: [{columnReferences: [], transform: \"\"}]`\n   - No `sourceMapping`\n\n2. **Stage nodes** (I_, S_):\n   - `type: sql`, `sqlType: Stage`\n   - `sourceMapping` with proper `aliases`, `join.joinCondition` using FROM/JOIN syntax with ref() and quoted aliases\n   - Each column's `sourceColumnReferences` links to the upstream source column UUID\n   - `config: {truncateBefore: true, insertStrategy: INSERT, testsEnabled: true}`\n\n3. **Dimension nodes** (D_):\n   - `type: sql`, `sqlType: Dimension`\n   - System columns: DIM_KEY (isSurrogateKey), SYSTEM_VERSION, SYSTEM_CURRENT_FLAG, SYSTEM_START_DATE, SYSTEM_END_DATE, SYSTEM_CREATE_DATE, SYSTEM_UPDATE_DATE\n   - Business key columns get `isBusinessKey: true`\n   - `sourceMapping` with FROM ref to upstream stage\n\n4. **View nodes** (Dim_):\n   - `type: sql`, `sqlType: View`, `materializationType: view`\n   - Source from the base dimension\n\n5. **Fact nodes** (F_):\n   - `type: sql`, `sqlType: Fact`\n   - System columns: SYSTEM_CREATE_DATE, SYSTEM_UPDATE_DATE\n   - `sourceMapping` with FROM/JOIN to summary stage\n\n**Lineage (from WhereScape procedure analysis):**\n```\nL_Email_SurveyDataDictionary (Source)\n  → I_NorthpowerNetworkSurveyMerge (Stage)\n    → I_NorthpowerNetworkSurvey (Stage)\n      → D_NorthpowerNetworkSurveyComments (Dim)\n      → D_NorthpowerNetworkSurveyDetails (Dim)\n      → D_NorthpowerNetworkSurveyQuestions (Dim)\n      → S_NorthpowerNetworkSurvey (Stage - joins I_ with all 3 D_ dims + D_Date)\n        → F_NorthpowerNetworkSurvey (Fact)\n```\n\n**JOIN conditions from procedures:**\n- `I_NorthpowerNetworkSurvey`: `FROM I_NorthpowerNetworkSurveyMerge LEFT JOIN I_NorthpowerNetworkSurveyQuestions ON QuestionNumber, ResponseCode, Wave, SurveyType`\n- `S_NorthpowerNetworkSurvey`: `FROM I_NorthpowerNetworkSurvey LEFT JOIN D_NorthpowerNetworkSurveyComments ON QuestionNumber, SurveyNumber, SurveyType LEFT JOIN D_NorthpowerNetworkSurveyDetails ON SurveyNumber, SurveyType LEFT JOIN D_NorthpowerNetworkSurveyQuestions ON QuestionNumber, ResponseCode, SurveyType, Wave LEFT JOIN D_Date ON WaveDate = CalendarDate`\n- `F_NorthpowerNetworkSurvey`: `FROM S_NorthpowerNetworkSurvey`\n\n### Step 3: Generate nodes\nRun the updated parser to generate ~10 nodes:\n- 1 Source: `BRONZE-L_EMAIL_SURVEYDATADICTIONARY`\n- 3 Stages: `SILVER-I_NORTHPOWERNETWORKSURVEYMERGE`, `SILVER-I_NORTHPOWERNETWORKSURVEY`, `SILVER-S_NORTHPOWERNETWORKSURVEY`\n- 3 Dimensions: `GOLD-D_NORTHPOWERNETWORKSURVEYCOMMENTS`, `GOLD-D_NORTHPOWERNETWORKSURVEYDETAILS`, `GOLD-D_NORTHPOWERNETWORKSURVEYQUESTIONS`\n- 3 Views: `GOLD-DIM_NORTHPOWERNETWORKSURVEYCOMMENTS`, `GOLD-DIM_NORTHPOWERNETWORKSURVEYDETAILS`, `GOLD-DIM_NORTHPOWERNETWORKSURVEYQUESTIONS`\n- 1 Fact: `GOLD-F_NORTHPOWERNETWORKSURVEY`\n\n### Step 4: Validate\nRun `coa validate` to confirm zero new errors.\n\n## Verification\n1. `coa validate` passes\n2. Spot-check that `sourceColumnReferences` in STG nodes point to correct Source column UUIDs\n3. Spot-check that Dimension `sourceMapping.join.joinCondition` contains proper FROM/JOIN ref syntax\n4. Verify Fact's sourceMapping references S_NORTHPOWERNETWORKSURVEY\n\n## Critical Files\n- `nodes/SILVER-STG_CUSTOMER.yml` — Pattern to follow for multi-source JOIN syntax, aliases, sourceColumnReferences\n- `nodes/SILVER-DIM_CUSTOMER.yml` — Pattern for Dimension with system columns, business keys\n- `nodes/BRONZE-CUSTOMER.yml` — Pattern for Source node with empty sourceColumnReferences\n- `scripts/parse_wherescape.py` — The file to rewrite\n- `nodes/SILVER-FCT_ORDERS.yml` — Pattern for Fact with system columns and single-source mapping