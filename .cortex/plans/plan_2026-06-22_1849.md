---
created: 2026-06-22T18:49:58.714Z
session: 76fec7f5-4829-47aa-9490-e96534a04ed3
working_directory: /Users/dougy/work/POC/Northpower/migration_poc
---

# WhereScape RED to Coalesce Migration - Network Survey POC\n\n## Context\n\n**What I explored:**\n- WhereScape RED deployment files (`app_FullMetaData/*.wst`) - semicolon-delimited object lists, UTF-16LE encoded SQL data file (164MB)\n- Existing Coalesce workspace at `~/work/POC/Northpower/migration_poc/` with V1 node types defined (Stage, Dimension, Fact, View, PersistentStage)\n- `coa` CLI v7.34.1 schema for node YAML files\n- The Network Survey subject area: 1 Load, 3 Stage (type 7), 3 Dimensions (type 6), 3 Dim Views (type 12), 1 Fact (type 5), 5 Data Stores (type 18)\n\n**Key findings:**\n- WhereScape types: `8`=Load, `7`=Stage, `6`=Dimension, `12`=Dim View, `5`=Fact, `18`=Data Store, `1`=Procedure, `10`=Index, `24`=Subgraph, `23`=Cube/View\n- Object types 7 and 18 both use `ws_stage_col` for columns; type 8 uses `ws_load_col`; type 6 uses `ws_dim_col`; type 5 uses `ws_fact_col`\n- The `app_data_` file is UTF-16LE encoded, contains INSERT statements into WhereScape metadata tables\n- All I_ tables use TRUNCATE/INSERT (Stage pattern), MERGE only appears in D_ dimension updates\n- Coalesce node files go in `nodes/` directory, named `<LOCATION>-<NAME>.yml`\n- Data types need mapping: SQL Server (`varchar(max)`, `integer`, `datetime`, `nchar(N)`, `numeric(38)`) → Snowflake (`VARCHAR`, `NUMBER`, `TIMESTAMP`, `VARCHAR(N)`, `NUMBER(38,0)`)\n\n## Implementation Steps\n\n### Step 1: Create a Python parser script\nCreate `scripts/parse_wherescape.py` that:\n1. Reads `app_obj_NP_FullMetaData.wst` (UTF-8 semicolon-delimited) to build object registry: `{obj_id: {type, name}}`\n2. Reads `app_data_NP_FullMetaData.wst` (UTF-16LE) and extracts:\n   - `ws_load_tab` / `ws_load_col` → Source node metadata\n   - `ws_stage_tab` / `ws_stage_col` → Stage node metadata (type 7)\n   - `ws_view_tab` / `ws_stage_col` → Stage node metadata (type 18, also uses `ws_stage_col`)\n   - `ws_dim_tab` / `ws_dim_col` → Dimension node metadata\n   - `ws_fact_tab` / `ws_fact_col` → Fact node metadata\n3. Filters to Network Survey objects only (names matching `*NorthpowerNetworkSurvey*` or `*NPNetworkSur*` or `L_Email_SurveyDataDictionary`)\n\n### Step 2: Data type mapping\nMap SQL Server types to Snowflake:\n- `integer` → `NUMBER`\n- `varchar(N)` → `VARCHAR(N)`\n- `varchar(max)` → `VARCHAR(16777216)`\n- `nchar(N)` / `nvarchar(N)` → `VARCHAR(N)`\n- `datetime` → `TIMESTAMP`\n- `date` → `DATE`\n- `numeric(N)` / `numeric(N,M)` → `NUMBER(N)` / `NUMBER(N,M)`\n- `float` → `FLOAT`\n- `bit` → `BOOLEAN`\n\n### Step 3: Generate Coalesce node YAML files\nCreate `nodes/` directory with these files for the Network Survey area:\n\n**Source (BRONZE):**\n- `BRONZE-L_EMAIL_SURVEYDATADICTIONARY.yml` — type=sourceInput, sqlType=Source\n\n**Stage (SILVER) — type 7 objects:**\n- `SILVER-I_NORTHPOWERNETWORKSURVEY.yml`\n- `SILVER-I_NORTHPOWERNETWORKSURVEYMERGE.yml`\n- `SILVER-S_NORTHPOWERNETWORKSURVEY.yml`\n\n**Stage (SILVER) — type 18 data stores:**\n- `SILVER-I_NORTHPOWERNETWORKSURVEYFAULTS.yml`\n- `SILVER-I_NORTHPOWERNETWORKSURVEYLINES.yml`\n- `SILVER-I_NORTHPOWERNETWORKSURVEYQUESTIONS.yml`\n- `SILVER-I_NORTHPOWERNETWORKSURVEYFIBRE.yml`\n- `SILVER-FACT_NORTHPOWERNETWORKSURVEY.yml` (type 18 named \"Fact_...\" but stored as data store)\n\n**Dimensions (GOLD) — type 6:**\n- `GOLD-D_NORTHPOWERNETWORKSURVEYCOMMENTS.yml`\n- `GOLD-D_NORTHPOWERNETWORKSURVEYDETAILS.yml`\n- `GOLD-D_NORTHPOWERNETWORKSURVEYQUESTIONS.yml`\n\n**Views (GOLD) — type 12 dim views:**\n- `GOLD-DIM_NORTHPOWERNETWORKSURVEYDETAILS.yml`\n- `GOLD-DIM_NORTHPOWERNETWORKSURVEYQUESTIONS.yml`\n- `GOLD-DIM_NORTHPOWERNETWORKSURVEYCOMMENTS.yml`\n\n**Fact (GOLD) — type 5:**\n- `GOLD-F_NORTHPOWERNETWORKSURVEY.yml`\n\n### Step 4: Node YAML structure\nEach node YAML follows this structure:\n```yaml\nname: D_NORTHPOWERNETWORKSURVEYCOMMENTS\nid: <generated-uuid>\nfileVersion: 1\ntype: Node\noperation:\n  locationName: GOLD\n  name: D_NORTHPOWERNETWORKSURVEYCOMMENTS\n  sqlType: Dimension          # Source|Stage|Dimension|Fact|View\n  type: sql                   # or \"sourceInput\" for Source nodes\n  isMultisource: false\n  materializationType: table  # or \"view\" for View nodes\n  config: {}\n  metadata:\n    sourceMapping:\n    - name: Step 1\n      join:\n        joinCondition: |\n          FROM {{ ref('SILVER', 'I_NORTHPOWERNETWORKSURVEY') }}\n      dependencies:\n      - locationName: SILVER\n        nodeName: I_NORTHPOWERNETWORKSURVEY\n    columns:\n    - name: D_NORTHPOWERNETWORKSURVEYCOMMENTS_KEY\n      dataType: NUMBER\n      description: \"Generated artificial key\"\n      columnReference: { stepCounter: \"1\", columnCounter: \"1\" }\n      isSurrogateKey: true\n      keyColumnType: surrogateKey\n    - name: QUESTION_NUMBER\n      dataType: VARCHAR(100)\n      description: \"Question Number\"\n      columnReference: { stepCounter: \"1\", columnCounter: \"2\" }\n      isBusinessKey: true\n      transform: \"\\\"QUESTIONNUMBER\\\"\"\n    ...\n```\n\nSource nodes use a simpler schema with `type: sourceInput` and no `sourceMapping`.\n\n### Step 5: Wire up lineage\nFor each object, use the `sc_src_table` / `dc_src_table` / `fc_src_table` fields to determine the upstream dependency:\n- Load tables → Source nodes (no upstream in Coalesce)\n- Stage I_ tables → depend on Load tables (BRONZE sources)\n- Summary S_ tables → depend on I_ Stage tables\n- Dimensions → depend on I_ Stage tables\n- Facts → depend on S_ Summary tables (using the `fc_src_strategy` join conditions)\n- Dim Views (type 12) → depend on Dimensions\n\n### Step 6: Validate\nRun `coa validate` to verify all generated YAML is schema-valid.\n\n## Verification\n1. `coa validate --dir .` — all nodes pass schema validation\n2. `coa create --dry-run` — DDL renders without errors\n3. Spot-check that column data types are valid Snowflake types\n4. Verify lineage: each Stage references its Source, each Dim references its Stage, each Fact references its Summary\n\n## Critical Files\n- `/Users/dougy/work/POC/Northpower/app_FullMetaData/app_obj_NP_FullMetaData.wst` — Object registry (type;id;name)\n- `/Users/dougy/work/POC/Northpower/app_FullMetaData/app_data_NP_FullMetaData.wst` — Column metadata (UTF-16LE, 164MB)\n- `/Users/dougy/work/POC/Northpower/migration_poc/nodeTypes/Dimension-Dimension/definition.yml` — V1 node type template pattern to follow\n- `/Users/dougy/work/POC/Northpower/migration_poc/locations.yml` — Available locations (BRONZE, SILVER, GOLD)\n- `scripts/parse_wherescape.py` — New parser script to create