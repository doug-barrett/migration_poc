fileVersion: 1
id: "5"
isDisabled: false
metadata:
  defaultStorageLocation: SILVER
  error: null
  nodeMetadataSpec: |
    capitalized: Custom
    short: CSM
    plural: Customs
    tagColor: 'pink'

    config:
    - groupName: Options
      items:
      - type: materializationSelector
        default: table
        options:
        - table
        isRequired: true
        enableIf: "false"

      - displayName: SQLSteps
        attributeName: SQL
        type: textBox
        syntax: sql
        isRequired: true
        default: "{{stage('Step 1')}}\nINSERT INTO {{this}} SELECT 1 AS col"

      - displayName: Enable Tests
        attributeName: testsEnabled
        type: toggleButton
        default: true
name: Custom
type: NodeType
